{% extends 'base.html.twig' %}

{% block title %} {% endblock %}

{% block body %}

    <h1> Zmena kategorie produktu</h1>
{{ form_start(form) }}
<table class="table">
    <tbody>
    <tr>


        {{ form(form) }}
        {#<td> {{ form_widget(form.category) }}<br>#}
            {#{{ form_errors(form.category) }}</td>#}
    </tr>
    </tbody>
</table>
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

{% endblock %}
