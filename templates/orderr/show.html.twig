{% extends 'base.html.twig' %}

{% block title %}Orderr{% endblock %}

{% block body %}
    <h1>Orderr</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ orderr.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ orderr.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('orderr_index') }}">back to list</a>

    <a href="{{ path('orderr_edit', {'id': orderr.id}) }}">edit</a>

    {{ include('orderr/_delete_form.html.twig') }}
{% endblock %}
