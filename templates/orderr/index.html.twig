{% extends 'base.html.twig' %}

{% block title %}Orderr index{% endblock %}

{% block body %}
    <h1>Orderr index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for orderr in orderrs %}
            <tr>
                <td>{{ orderr.id }}</td>
                <td>{{ orderr.name }}</td>
                <td>
                    <a href="{{ path('orderr_show', {'id': orderr.id}) }}">show</a>
                    <a href="{{ path('orderr_edit', {'id': orderr.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('orderr_new') }}">Create new</a>
{% endblock %}
